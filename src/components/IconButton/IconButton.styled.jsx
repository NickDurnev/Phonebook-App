import styled from 'styled-components';

const Wrap = styled.button`
  width: ${({ width }) => width};
  padding: 0;
  pointer-events: auto;
  color: inherit;
  background-color: inherit;
`;

export default Wrap;
