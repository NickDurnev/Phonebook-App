// const getFilter = rootReducer => rootReducer.filter;

// const getFilteredContacts = (contacts, rootState, favorite) => {
//   const filter = getFilter(rootState);
//   const normalizedFilter = filter.toLowerCase();
//   let contactsToFilter = contacts;
//   if (favorite) {
//     contactsToFilter = contacts.filter(contact => contact.favorite);
//   }
//   return contactsToFilter.filter(contact =>
//     contact.name.toLowerCase().includes(normalizedFilter)
//   );
// };

// export default getFilteredContacts;
